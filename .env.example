# WallRush environment variables

# Port (Railway sets this automatically)
PORT=3000

# Supabase (accounts, leaderboard). Optional: without these the game
# still works — guests only, no accounts / leaderboard.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=
